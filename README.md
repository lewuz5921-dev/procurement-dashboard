# 采购数字化数据看板 · Procurement Dashboard

> 🔗 **在线演示**：https://procurement-kpi-board-79919.app.workbuddy.host/

## 关于作者与这个项目（请先读这一节）

本项目作者为**物流管理专业在读学生，没有采购岗位从业经历**。

看板基于物流管理专业课程框架（采购管理 / 供应商管理 / 供应链成本）与公开行业资料搭建，数据为模拟数据（`scripts/gen_data.py` 生成，含刻意埋入的业务异常）。

因此本项目**不主张"还原了真实采购场景"**，而主张以下三件事：

1. **采购核心指标的口径设计与可解释性** —— 每个指标都能说清"为什么这样算、分母为什么是它"；
2. **异常检测采用确定性规则引擎**，每条预警可追溯到明确的触发条件，规则代码与对外声明的口径严格一致；
3. **LLM 只做意图解析，答案由指标函数计算** —— 不做任何无出处的数字生成。

附「[口径 revision：节约率从 v1 到 v2](#口径-revision节约率从-v1-到-v2)」一节，记录了一次**被自己发现的指标口径错误**及其修正过程。

---

## 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 前端 | Vue 3 + Vite | 组合式 API + 路由，组件化 |
| 可视化 | ECharts 5 | 柱/线/饼/散点/雷达/堆叠图 |
| 数据 | 本地 JSON 模拟集 | 由 `scripts/gen_data.py` 生成，1008 条订单（含物料与基准价字段）+ 20 家供应商 |
| 检测 | 规则引擎（纯 JS） | 可解释的确定性规则，见 `src/utils/rules.js` |
| AI | 大模型 + Function Calling（BYOK） | 自然语言问数：LLM 只做意图解析，答案来自指标函数；密钥由使用者自带，**构建产物不含任何密钥** |

**数据分层设计**：数据加载层（`src/data/store.js`）与指标计算层（`src/utils/metrics.js`）解耦。真实场景下，仅需将 `store.js` 中的本地 fetch 替换为后端 API（如 Java/SQL 数据服务），指标计算逻辑不变。

## 快速开始

```bash
npm install
npm run gen:data      # 重新生成模拟数据（可选，已内置；会打印节约率 v2 自检结果）
npm run dev           # 本地开发 http://localhost:5173
npm run build         # 生产构建，产物在 dist/
npm run preview       # 预览构建产物
```

**启用 AI 问答（BYOK，自带密钥）**：打开 `#/query` 页面右上角「API 设置」，填入你自己的 API Key 与接口地址即可。

- 密钥只写入**浏览器 localStorage**，不会上传、不进入构建产物；
- 未配置时页面显示「规则解析模式」，由关键词规则解析意图，**功能与答案口径完全不受影响**；
- 默认接口地址放在 `public/llm-config.json`（运行时读取、可自由改成自建代理地址；置空则必须由使用者自填）。

**⚠️ 请勿再使用 `VITE_*` 前缀的环境变量存密钥** —— Vite 会在编译期把它内联进静态产物，任何人打开开发者工具即可提取。详见下节。

## 安全说明：公开产物里的密钥是零

本节记录一次**真实发生过的密钥泄漏及其修复**，因为"如何确认没有泄漏"比"我保证没有泄漏"更值得写下来。

### 问题

早期版本用 `import.meta.env.VITE_DEEPSEEK_API_KEY` 读取密钥，且上游地址是硬编码字面量。Vite 会在**构建期把 `VITE_*` 变量内联进 JS 产物**，于是线上可下载的 `SmartQueryView-*.js` 分包里同时出现了 `sk-` 开头的密钥与上游域名。

一个容易被漏掉的细节：**只扫描主包 `index-*.js` 会得出"没泄漏"的错误结论**——密钥在懒加载路由分包里。结论的证据范围必须与结论的表述范围一致。

### 修复（BYOK）

1. 代码中**彻底移除**对 `VITE_*` 密钥变量的读取，密钥改由使用者运行时提供（`src/utils/llm.js` 的 `getKey/setKey`，存 localStorage）；
2. 上游地址移出 JS：放到 `public/llm-config.json`，运行时 `fetch` 读取，可改为自建代理；
3. 智能问答页明确区分「LLM 模式」与「规则解析模式」，并展示客户端「API 设置」入口。

### 如何复核（可复现）

```bash
npm run build
# 穷尽扫描全部产物，含懒加载分包
grep -rEn 'sk-[A-Za-z0-9_-]{16,}' dist/
grep -rn 'deepseek' dist/assets/
```

预期：`sk-` **零命中**；`deepseek` 在 `dist/assets/`（即全部 JS/CSS 分包）中**零命中**，仅存在于 `dist/llm-config.json` 这一份可编辑配置里。

> 若你的验收标准要求连域名也不出现在任何静态资源中，把 `public/llm-config.json` 的 `baseUrl` 置空即可 —— 页面会提示使用者自填接口地址。

## 页面信息架构（主线收敛）

六个页面全部保留，但**只有一条主线**，其余降级为下钻页 —— 因为看板的价值不在"能看多少"，而在"看完知道该干什么"。

```
采购总览（发生了什么） ──► 智能洞察（为什么） ──► 智能问答（直接问数）
   │                          │
   ├─ 准时交付率 OTD 卡 ──────► 履约监控（下钻，可带 ?supplier=A01 预选供应商）
   ├─ 成本节约卡 ────────────► 成本分析（下钻）
   ├─ 「口径说明 →」─────────► 智能洞察页内「成本节约率口径构成」区块（带落点高亮）
   ├─ 供应商帕累托卡 ────────► 供应商评估（下钻）
   └─ 异常预警卡 ────────────► 智能洞察（下钻）
```

- 首页第一屏的**每张 KPI 卡都自带"下一步去哪看"**，点击即下钻，不做平级导航；
- 第一屏下半部分最显眼的位置留给**一条单供应商的 OTD 趋势**（华信精密 91.1% → 13.6%），配一句可执行结论；
- 下钻页顶部均提供「← 返回总览」。

### 首屏只放结论，不放方法论

首屏按「结论层 → 证据层」组织，**不出现表格类元素**：

| 屏位 | 内容 | 抽象层 |
|---|---|---|
| KPI 卡区 | 采购总额 / OTD / 成本节约 / 异常预警 | 结论层 |
| 故事卡 | 华信精密断崖 + 可执行结论 | 证据层 |
| 图表区 | 月度趋势 / 品类结构 / 帕累托 | 明细层 |

- **口径构成表已迁至智能洞察页内**（作为页内区块，不新增路由，侧边栏仍为 3 项）。首屏改为一行摘要 + `口径说明 →` 次级链接，**一次点击直达**并高亮落点；
- **去重**：KPI 卡 2 只保留「低于目标 4.4pt」，归因交由故事卡承载 —— 华信精密的关键结论在首屏只出现一次（但卡片仍可点击下钻到该供应商）；
- **主行动收敛**：首屏只有 1 个主按钮样式元素，其余为次级文字链接。

## 指标口径表（面试核心）

| 指标 | 计算公式 | 业务含义 |
|------|---------|---------|
| 采购总金额 | Σ(单价 × 数量) | 采购规模 |
| 准时交付率 OTD | 按时到货订单数 / 总订单数 × 100% | 供应商交付可靠性 |
| **OTD 目标差距** | 目标 90% − 实际 OTD（百分点） | 还差多少才达标 |
| **OTD 拖累归因** | 某供应商逾期订单数 / 全量订单数 × 100（pt） | 谁在拉低整体 OTD，各项可加和、可排序 |
| 验收合格率 | 质量合格订单数 / 总订单数 × 100% | 来料质量水平 |
| 平均交付周期 | Σ实际交付天数 / 订单数 | 履约效率 |
| 供应商集中度 | TOP 供应商金额 / 品类总金额 | 依赖风险（>60% 预警） |
| **成本节约率 v2** | Σ(基准单价 − 成交单价) × 数量 ÷ Σ(基准单价 × 数量) | 相对独立基准价的议价收益，**分母用基准口径总额** |
| **基准覆盖率** | 有基准价的订单数 / 总订单数 | 该节约率能代表多少订单，**必须与节约率同时展示** |
| TCO 全生命周期成本 | 单价 × (1 + 运输费率 + 质量损失率 + 交易管理费率) | 打破「单价最低」直觉 |
| 帕累托累计占比 | 按金额降序累加 / 总额 | 识别核心供应商 |

### 金额格式规范（`src/utils/format.js`，全站唯一口径）

按量级分段决定精度，**避免 `¥279 → ¥0.0 万` 这类失真显示**：

| 量级 | 规则 | 示例 |
|---|---|---|
| ≥ 1 亿 | 亿元 + 2 位小数 | `¥1.23 亿` |
| ≥ 100 万 | 万元 + 1 位小数 | `¥1,234.5 万` |
| 1 万 ~ 100 万 | 万元 + 2 位小数 | `¥12.34 万` |
| < 1 万 | 元 + 千分位 | `¥5,909`、`−¥279` |

- 口径构成表主区（①②③与合计行）统一 `fmtMoneyWan()`：万元 + 1 位小数，该区各行金额均 ≥ 1 万；
- 样本不足的降级行（见下）为小字次级文本色，节约额列允许退回「元 + 千分位」；
- 页面内**禁止自行 `toFixed`** 拼金额，一律走 `format.js`。

### 基准价取数口径（四级优先级）

`scripts/gen_data.py` 的 `attach_benchmarks()` 为每笔订单挂载 `benchmarkPrice` + `benchmarkSource`：

| 优先级 | 基准来源 | 字段 | 取数规则 |
|---|---|---|---|
| ① | 年度框架合同价 | `contract_price` | 「品类 × 供应商」年初签订、全年固定、**不随行就市**；一旦有合同价优先使用 |
| ② | 上次成交价 | `last_deal_price` | 同物料 + 同供应商最近一次成交（**3 个月内**），按品类行情指数折算到本期口径 |
| ③ | 当期询价最低有效报价 | `best_quote` | 本期询价中的最低有效报价，围绕当期市场参考价竞争得出 |
| ④ | 滚动 12 个月成交均价 | `hist_avg_12m` | 同物料 + 同供应商近 12 个月成交均价（**不含当期**，样本 ≥3 笔），折算到本期口径 |

三条硬约束：

- **基准不与被评估订单同源。** 基准取自其他时点 / 其他主体 / 事先约定的价格，按时间顺序逐单取数、**不使用本单之后的信息**（无前视偏差）；
- **无基准价的订单不参与计算、也不补默认值。** 宁可少算，不可假算；
- **覆盖率低于 60% 时直接显示「基准价缺失，暂不可计算」**，不用 0 或均价顶上。假数字比没有数字更危险。

## 异常检测规则引擎

| 规则 | 触发条件 | 阈值 |
|------|---------|------|
| R1 OTD 连续下滑 | 近 3 个月 OTD 较前 9 个月基线下降 | > 20 个百分点 |
| R2 报价离群 | 供应商成交均价 vs **同品类其他供应商**均价（**留一法**，剔除自身样本） | > 2σ |
| R3 单一供应商依赖 | 单一供应商占品类采购额 | > 60% |
| R4 低价高 TCO | 单价低于品类均值 15% 且 TCO 高于品类均值 | 双条件 |

> R2 的「留一法」是修正后的口径：把候选供应商自己的订单算进品类 σ，它会把自己的离群"洗白"
> —— 实测鑫源材料（A12）在含自身口径下仅 **+1.50σ**（不触发），留一法口径下为 **+2.57σ**（触发）。
> 规则代码必须与对外声明的口径严格一致，这一条已写进 `src/utils/rules.js` 的注释。

### 风险分级依据（页面同步披露，与代码一致）

各规则触发量纲不同（百分点 / 集中度 / 标准差 / TCO 金额），因此**不做跨规则的统一数值排序**。「高风险」只表示**在该规则内已达需立即处置的程度**：

| 规则 | 等级 | 判定条件 | 影响金额口径 |
|---|---|---|---|
| R1 | 高风险 | 近 3 个月 OTD < 40% | 近 3 个月逾期订单的金额合计 |
| R1 | 中风险 | 近 3 个月 OTD ≥ 40% | 同上 |
| R2 | 中风险 | 命中即中风险（+2σ 留一法） | （成交均价 − 同品类其他供应商均价）× 已采购数量 |
| R3 | 高风险 | 命中即高风险（集中度 > 60%） | 该供应商在该品类的采购额（断供敞口） |
| R4 | 中风险 | 单价 < 品类均价 85% 且 TCO 高于品类均值 | （TCO − 品类 TCO 均值）× 已采购数量 |

> 上述阈值**是从既有实现反推确认**的，不是先写文档再改代码 —— 按此表复算，「7 条预警 = 高风险 2 + 中风险 5」逐条吻合，无例外。
> 影响金额仅用于**同一等级内**排序，不跨规则比较。

### R1 归因文案按类型分型

同样叫「OTD 连续下滑」，下降 20.6pt 与 77.4pt 的业务性质完全不同（渐进劣化 ≠ 交付中断）。单一模板会让"归因分析"退化成填空，因此按顺序匹配、命中即停：

| 顺序 | 类型 | 判定条件 | 文案方向 |
|---|---|---|---|
| 1 | **断崖型** | 近 3 个月 OTD < 40% | 交付中断：核查在手订单、启动备选供应商切换 |
| 2 | **高位回落型** | 基线 OTD ≥ 95% 且近 3 个月 OTD ≥ 50% | 是否新增订单导致超载、基线是否异常偏离 |
| 3 | **渐进型** | 下降 20–40pt 且近 3 个月 OTD ≥ 50% | 产能利用率与排产计划核查 |
| 4 | 观察型 | 其他 | 按周跟踪，再决定处置力度 |

优先级不可省略：瑞泰电子（基线 100.0%、下降 28.6pt）会同时命中「高位回落型」与「渐进型」。

**每条文案都嵌入该供应商的实际数值**（基线 / 近 3 月 OTD / 下降幅度 / 近期单数与逾期数 / 备选供应商名），因此同型共用模板也不会出现逐字相同的文本。备选供应商由数据动态推导，不写死。

每条预警均含「归因分析 + 优化建议」，可追溯到明确触发条件（可解释 AI 原则）。

## 埋入的演示异常（供演示讲解）

当前全量数据下规则引擎共触发 **7 条预警**（R1×4 / R2×1 / R3×1 / R4×1）。页面列表按「风险等级降序 → 影响金额降序」排序：

| 供应商 | 风险 | 现象 | 触发规则 / 分型 | 影响金额 |
|--------|------|------|----------------|---------|
| **正扬微电子（A07）** | 高 | 电子元器件品类 **84.3%** 采购额集中 | R3 | ¥602.6 万 |
| **华信精密（A01）** | 高 | 近 3 个月 OTD 由 91.1% 降至 13.6%，19 单逾期 | R1 · **断崖型** | ¥123.3 万 |
| **蓝海智造（A03）** | 中 | 结构件品类单价最低（低于品类均价约 17%），但运输 + 质量损失导致 TCO 最高 | R4 | ¥32.13 万 |
| **鑫源材料（A12）** | 中 | 成交均价高于同品类其他供应商 **+2.57σ**（留一法口径） | R2 | ¥25.44 万 |
| 瑞泰电子（A06） | 中 | 基线 OTD 100.0% → 71.4%，下降 28.6pt | R1 · **高位回落型** | ¥5.34 万 |
| 佳信化工（A10） | 中 | 基线 OTD 93.3% → 72.7%，下降 20.6pt | R1 · **渐进型** | ¥4.97 万 |
| 安捷包装（A15） | 中 | 基线 OTD 83.3% → 56.2%，下降 27.1pt | R1 · **渐进型** | ¥3.93 万 |

## 口径 revision：节约率从 v1 到 v2

> 这一节记录了一次**被自己发现的口径错误**。保留错误、写清修正过程，是因为它比"结果好看"更能说明方法是否可靠。

### v1 口径（已废弃）

```
节约率 = Σ(品类均价 − 成交单价) × 数量 / 总金额
```

它站不住的原因不是"数字偏大偏小"，而是**数学上恒等于 0**。品类均价本身就是这批订单的数量加权均价，代进去：

```
Σ(均价 − 单价ᵢ) × 数量ᵢ
= 均价 × Σ数量ᵢ − Σ(单价ᵢ × 数量ᵢ)
= Σ(单价ᵢ × 数量ᵢ) − Σ(单价ᵢ × 数量ᵢ)
= 0
```

即「拿自己当基准量自己」。而线上页面当时显示的是 **0.3%** 而不是 0% —— 这个差值本身就是一个信号：说明中间有一步不是数量加权（当时实现用的是**未加权算术平均**，与分母的加权口径不一致）。不管哪种情况，这个数字都**不能作为"节约"对外呈现**。

更根本的问题：真实采购不拿"当期均价"当基准。拿一批订单自己的平均值当靶子，这叫自说自话。

### v2 口径（现行）

引入独立的**基准单价 `benchmark_price`**，四级优先级取数（见上文「基准价取数口径」）：

```
节约额 = Σ(基准单价ᵢ − 成交单价ᵢ) × 数量ᵢ          （仅计有基准价的订单）
节约率 = 节约额 / Σ(基准单价ᵢ × 数量ᵢ)              （分母用基准口径总额）
```

**分母为什么用基准口径总额？** 它代表"不省的话要花多少"。若改用实际总额，分母更小、节约率会被放大 —— 两种口径行业内都有人用，**本项目的选择是基准口径总额，并在此写明**。

**为什么 v2 不会重蹈恒等于 0 的覆辙？** 关键不是"基准是否来自本项目的数据集"（模拟数据不可能有外部真值），而是**基准不与被评估订单构成同一求和项**：① 是事先约定的合同价，②④ 取自**其他时点**的成交，③ 取自**当期市场**的最低报价。它们都不是"这一单自己"。

### 覆盖率机制（与 v2 同时引入）

不是每笔订单都有基准价，所以**报节约率时必须同时报覆盖率**：

```
节约率 1.78%（基准覆盖率 77.8%，224 单无基准价未计入）
```

- 无基准的订单**不参与**节约率计算，也**不补默认值**（不用 0、不用均价顶上）；
- 覆盖率低于 60% 时，页面直接显示 `基准价缺失，暂不可计算` —— 假数字比没有数字更危险；
- 覆盖率本身也要能解释：模拟数据里它由三个参数决定（`scripts/gen_data.py` 顶部常量，可调参复现）：
  `FRAME_P = 0.52`（品类×供应商已签框架协议比例）、`RECORD_P = 0.65`（该组合是否已维护基准价档案）、
  `RFQ_P = 0.55`（物料是否纳入询价池）。现实中基准价不是自动就有的，需要采购员在系统里维护，
  维护完整度低正是本页面强制显示覆盖率的原因。

### 拆开看才有信息量

单一节约率数字藏得住问题，拆开就藏不住 —— 当前数据的四类基准口径结果完全不同：

| 基准来源 | 订单数 | 基准金额 | 节约额 | 节约率 | 说明 |
|---|---|---|---|---|---|
| ① 年度框架合同价 | 441 | ¥983.2 万 | ¥38.3 万 | **+3.90%** | 合同价口径的降本率 |
| ② 上次成交价 | 181 | ¥568.1 万 | ¥0.6 万 | **+0.10%** | 剔除行情后与上次基本持平 → 议价能力没有改善 |
| ③ 当期询价最低报价 | 159 | ¥410.2 万 | −¥3.9 万 | **−0.95%** | 成交价**高于**当期最低有效报价 → 询价环节仍有下探空间 |
| ④ 滚动 12 个月均价 | *3* | ¥2.4 万 | *−¥279* | *−1.17%* | *样本不足（3 单），仅供参考 —— 页面降级为小字次级文本色* |
| **合计（已覆盖）** | **784** | **¥1963.9 万** | **¥35.0 万** | **+1.78%** | 覆盖率 77.8%，224 单无基准价未计入 |

> ④ 行只有 3 单却与 441 单并列同字号，会让读者误以为四类基准的结论等权重。**样本量 < 10 单的行一律降级**：字号小一级、次级文本色、行内标注「样本不足（N 单），仅供参考」，金额退回「元 + 千分位」以免出现 `−¥0.0 万`。

### 一句话结论

v1 错的不是"算得不准"，而是**结构上不可能算对**；v2 换了基准，并且强制把"能代表多少订单（覆盖率）"和"每一类基准各自表现如何（分来源）"一起报出来 —— 一个不敢拆开的指标，本身就不该被信任。

## 系统架构

```
┌─────────────────────────────────────────────────┐
│              前端展示层 (Vue3 + ECharts)           │
│ 主线下钻: 总览 → 智能洞察 → 智能问答                 │
│ 下钻页:   履约监控 / 成本分析 / 供应商评估            │
└──────┬───────────────────────────────┬──────────┘
       │ fetch (可替换为后端 API)         │ 自然语言提问
┌──────▼──────────────────────┐  ┌─────▼──────────┐
│   指标计算层 (metrics.js)     │  │ AI 意图解析层    │
│  OTD/TCO/帕累托/品类/节约率v2  │  │ (llm.js)        │
│  + 覆盖率 + OTD拖累归因        │  │ DeepSeek FC     │
│   ▲ 供查询执行层调用           │  │ 仅输出查询参数    │
└──────┬──────────────────────┘  └─────┬──────────┘
       │                               │
┌──────▼──────────────────────┐       │
│   规则引擎层 (rules.js)       │       │
│  OTD骤降/报价离群(留一法)/     │       │
│  单一依赖/低价高TCO            │       │
└──────┬──────────────────────┘       │
       │                              │
┌──────▼──────────────────────┐       │
│   查询执行层 (queryEngine.js) │◄──────┘
│  执行结构化查询 + 模板拼答案    │
└──────┬──────────────────────┘
       │
┌──────▼──────────────────────┐
│  数据层 (store.js / 本地 JSON)│
│  供应商 / 订单(含基准价) 模拟集 │
└─────────────────────────────┘
```

## 目录结构

```
src/
├── App.vue              # 布局（侧边栏：3 项主线）
├── main.js
├── router/index.js      # 6 个页面路由（下钻页仍可 URL 直达）
├── data/store.js        # 数据加载 + 全局筛选
├── utils/
│   ├── metrics.js       # 指标口径（节约率 v1/v2、覆盖率、OTD 拖累归因）
│   ├── format.js        # 金额格式化（按量级分段，全站唯一口径）
│   ├── rules.js         # 异常检测规则引擎（R2 留一法 / R1 分型 / 分级依据）
│   ├── llm.js           # 意图解析（Function Calling，BYOK）+ 关键词规则降级
│   ├── queryEngine.js   # 查询执行 + 答案模板
│   └── useChart.js      # ECharts 组合式封装
└── views/
    ├── OverviewView.vue     # 总览（主线入口：3+1 KPI 卡 + 故事卡）
    ├── SupplierView.vue     # 供应商评估（Kraljic + 雷达）— 下钻页
    ├── FulfillmentView.vue  # 履约监控（OTD + 预警，支持 ?supplier= 参数）— 下钻页
    ├── CostView.vue         # 成本分析（TCO 对比）— 下钻页
    ├── InsightView.vue      # 智能洞察（规则归因 + 分级依据 + 口径构成表）
    └── SmartQueryView.vue   # 智能问答（自然语言问数 + API 设置）

public/
└── llm-config.json      # 上游接口默认地址（运行时读取，可改自建代理）
```

### 订单数据字段

存量字段（`id / month / date / supplierId / supplierName / category / qty / unitPrice / amount / leadDays / actualLeadDays / onTime / qualityPass`）之外，本次为节约率 v2 新增 3 个字段：

| 字段 | 含义 | 缺省值 |
|---|---|---|
| `material` | 物料规格档（每品类 4 档，按基准口径单价分位确定） | 必有 |
| `benchmarkPrice` | 基准单价；取不到时为 `null` | 可为 `null` |
| `benchmarkSource` | 基准来源枚举（`contract_price` / `last_deal_price` / `best_quote` / `hist_avg_12m`） | 可为 `null` |

> 生成器改造时使用了**独立随机流**（`random.Random(2026)`），因此新增字段不影响既有成交数据的随机序列——存量 1008 单的单价 / 数量 / 履约表现**逐位未变**。

## 部署

构建产物为纯静态文件（`dist/`），可部署到任意静态托管：GitHub Pages / Vercel / Netlify / Cloudflare Pages。

- GitHub Pages：仓库 Settings → Pages → 选择 `main` 分支 `/docs` 或启用 Actions 部署 `dist/`
- 注意 `vite.config.js` 已设置 `base: './'`，支持子路径部署
- `vite.config.js` 的 `server` / `preview` 均设了 `host: '0.0.0.0'` + `allowedHosts: true`，否则经反向代理访问会被 Vite 以 `Blocked request` 拒绝

### 部署前安全自检（建议固化为 CI 步骤）

```bash
npm run build
grep -rEn 'sk-[A-Za-z0-9_-]{16,}' dist/ && exit 1 || echo "OK: 无密钥特征"
grep -rn 'deepseek' dist/assets/ && exit 1 || echo "OK: JS/CSS 分包无上游域名"
```

密钥策略：**密钥只存在于使用者的浏览器，服务端与产物中均无**。若后续要为团队共用而引入服务端代理，代理地址直接改 `public/llm-config.json` 即可，前端代码无需改动。
